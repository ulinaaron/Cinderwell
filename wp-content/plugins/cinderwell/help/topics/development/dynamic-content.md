Register dynamic sources with `cinderwell_data_sources` and resolve their values with `cinderwell_resolve_data_source`. Extend conditional display choices with `cinderwell_conditions` and evaluate custom rules with `cinderwell_condition_evaluate`.

Return escaped, context-appropriate values and define a safe fallback when source data is empty. Keep business data in its source of truth rather than duplicating it in page content.
