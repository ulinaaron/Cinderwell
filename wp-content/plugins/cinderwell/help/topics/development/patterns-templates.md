Add client compositions to `patterns/`, full templates to `templates/`, and header or footer markup to `parts/` in the child theme. Patterns are best for starting layouts; templates define persistent site structure.

Site Editor saves are stored in the database and override theme files. Before shipping an update, review customized templates, export intentional changes, and decide whether each change belongs in the child theme or only on that site.
