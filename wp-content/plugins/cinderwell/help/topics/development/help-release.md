Register client-theme documentation on `cinderwell_register_documentation`. Topics belong in the theme’s `help/` directory so the Help screen, release tooling, and future documentation consumers all read the same source.

Before release, build assets, lint PHP and JavaScript, test editor and front-end behavior at desktop and mobile widths, and export intentional Site Editor changes. Version and deploy the core plugin, add-ons, and client theme as separate layers.
